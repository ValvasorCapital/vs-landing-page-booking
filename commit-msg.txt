fix: tighten Cal.com widget container on desktop

- Removed redundant dark container wrapper (bg, border, shadow) around Cal widget
- Removed forced min-height to let widget size naturally
- Reduced max-width from 6xl to 5xl for a flush, premium feel